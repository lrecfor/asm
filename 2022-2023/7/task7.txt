Задания.

++1. Для структуры 
string struct
  str_length	dword ?
  str_ptr	dword ?
string ends
реализовать стандартные строковые функции (без использования строковых инструкций):
+strstr	 
+strcpy 	
+strlen 	
+strcat 	
+strcmp 	
+strchr 
+strncpy	
+strnlen	
+strncat 
+strncmp 
1 балл.


++2. Реализовать стандартные строковые функции с помощью строковых инструкций (для строк char*). (checked)
1 балл.


3. Реализовать динамические структуры данных и программы для их тестирования.
++1) Сортировка списка строк.
++2) Двоичное дерево поиска. Реализовать операции вставки по ключу и поиска по ключу. Ключом и значением являются строки.
++3) Хеш-таблица. Реализовать операции вставки по ключу и поиска по ключу. Ключом и значением являются строки.
По 2 балла.


4. Дописать пример examples\winapi\1.
1) Реализовать копирование файла с помощью вызовов ReadFile/WriteFile (поблочно, весь файл в память не помещается). Выделять память для блока в новой куче с помощью HeapCreate и HeapAlloc.
2) Реализовать копирование файла с помощью проекций файлов в память (входной файл проецируется с правом на чтение, выходной файл проецируется с правом на запись). Считаем, что весь файл в память не проецируется. Поэтому проецируем блоками некоторого размера.
1 балл.




	mov ebx, pstr1
	mov ebx, [ebx].string.str_ptr
	mov ecx, pstr2
	mov ecx, [ecx].string.str_ptr
	
	mov dl, byte ptr [ebx]

	.while 1
		mov dl, byte ptr [ebx]
		mov dh, byte ptr [ecx]
	
		.break .if dl == 0
		.break .if [maxlen] == 0
	
		inc ebx
		inc ecx
		dec [maxlen]
	
		.if dl < dh
			mov eax, -1
			ret
		.elseif dl > dh
			mov eax, 1
			ret
		.endif

	.endw
	
	.if dh == 0
		mov eax, -1
	.else
		mov eax, 0
	.endif
	
    ret
